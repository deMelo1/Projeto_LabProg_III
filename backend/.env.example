# As variaveis de ambiente foram consolidadas na raiz do projeto.
# Veja ../.env.example para o template completo (com variaveis do banco,
# portas e URL da API consumida pelo frontend).
#
# Em desenvolvimento local sem Docker, o backend tambem aceita:
#   DATABASE_URL=postgresql://eco:eco@localhost:5432/ecofilter
#   UPLOAD_DIR=./uploads
